# LeanVibe Agent Hive - Centralized Port Configuration
# Non-standard ports to avoid conflicts with other development projects

# Database Services
POSTGRES_PORT=5433          # Non-standard to avoid conflicts (standard: 5432)
POSTGRES_TEST_PORT=5434     # For test isolation
REDIS_PORT=6380             # Non-standard to avoid conflicts (standard: 6379)
REDIS_TEST_PORT=6381        # For test isolation

# API Services
MAIN_API_PORT=8100          # Non-standard to avoid conflicts (standard: 8000)
PROJECT_INDEX_PORT=8101     # Project indexer API
MOBILE_BACKEND_PORT=8102    # Mobile PWA backend
TEST_API_PORT=8103          # For test isolation

# Frontend Services
FRONTEND_DEV_PORT=5173      # Vite default, non-standard
DASHBOARD_PORT=3001         # Non-standard to avoid conflicts (standard: 3000)
PWA_DEV_PORT=3002          # PWA development server

# Monitoring Services
PROMETHEUS_PORT=9090        # Standard (rarely conflicts in dev)
GRAFANA_PORT=3101           # Non-standard to avoid frontend conflicts
ALERTMANAGER_PORT=9093      # Standard

# Exporters (Monitoring)
POSTGRES_EXPORTER_PORT=9187 # Standard
REDIS_EXPORTER_PORT=9121    # Standard
NODE_EXPORTER_PORT=9100     # Standard
CADVISOR_PORT=8080          # Standard (may conflict, consider 8180)

# Management Tools
PGADMIN_PORT=5150           # Non-standard (standard external would be 5050)
REDIS_INSIGHT_PORT=8201     # Non-standard
JUPYTER_PORT=8888           # Standard

# Logging Stack
ELASTICSEARCH_PORT=9200     # Standard
LOGSTASH_BEATS_PORT=5044    # Standard
LOGSTASH_TCP_PORT=5000      # Standard
LOGSTASH_API_PORT=9600      # Standard
KIBANA_PORT=5601            # Standard

# Web Server
HTTP_PORT=80                # Standard
HTTPS_PORT=443              # Standard

# Development/Testing Ports
WEBPACK_DEV_PORT=3003       # For any webpack dev servers
STORYBOOK_PORT=6006         # Standard storybook port
MOCK_SERVER_PORT=8300       # For API mocking in tests

# Project Index Specific
PROJECT_INDEX_METRICS_PORT=9101    # Metrics for project indexer
PROJECT_INDEX_WEBSOCKET_PORT=8104  # WebSocket connections

# Load Balancer/Proxy
NGINX_HTTP_PORT=8000        # External nginx HTTP
NGINX_HTTPS_PORT=8443       # External nginx HTTPS (non-standard to avoid conflicts)

# High-availability ports (for clustering)
POSTGRES_REPLICA_PORT=5435  # PostgreSQL replica
REDIS_SENTINEL_PORT=26379   # Redis Sentinel

# Development overrides
DEV_MODE_PORT_OFFSET=1000   # Add to base ports in dev mode for complete isolation